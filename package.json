{
  "name": "@fnmain/prisma-util",
  "version": "1.0.1",
  "description": "prisma util",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node dist/test.js",
    "pub": "npm run build && npm publish --access public --registry=https://registry.npmjs.org/ && node sync-npmmirror.js "
  },
  "author": "DC",
  "license": "MIT",
  "files": [
    "dist",
    "src"
  ],
  "devDependencies": {
    "@prisma/client": "^5.10.2",
    "@types/node": "^20.8.3",
    "prisma": "^5.10.2",
    "typescript": "^5.2.2"
  }
}
